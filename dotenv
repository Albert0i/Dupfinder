HOST=localhost
PORT=3000

# SQLite DB path
DB_PATH='./data/db.sq3'

# Batch size for inserts and updates
BATCH_SIZE=1000

# Max return limit
MAX_LIMIT=9999

# Max number of workers in concurrent mode
MAX_WORKERS=8

# For prisma
DATABASE_URL='file:../data/db.sq3'
